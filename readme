Running
Replace example-api-key with your actual OpenAI API key.
docker compose up --build

Open frontend at: http://localhost:3000
Backend runs at: http://localhost:5001
Stop containers: docker compose down
Rebuild frontend without cache: docker compose build --no-cache frontend